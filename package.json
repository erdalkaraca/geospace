{
  "name": "geospace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@kispace-io/app",
    "build": "npm run build --workspace=@kispace-io/app",
    "preview": "npm run preview --workspace=@kispace-io/app",
    "lint": "echo 'No linting configured yet'",
    "type-check": "npm run type-check --workspace=@kispace-io/app",
    "ci": "npm run type-check",
    "validate-ci": "node scripts/validate-ci.js"
  },
  "dependencies": {
  }
}
