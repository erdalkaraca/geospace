{
  "name": "geospace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@kispace-io/geospace",
    "build": "npm run build --workspace=@kispace-io/geospace",
    "preview": "npm run preview --workspace=@kispace-io/geospace",
    "lint": "echo 'No linting configured yet'",
    "type-check": "npm run type-check --workspace=@kispace-io/geospace",
    "ci": "npm run type-check",
    "validate-ci": "node scripts/validate-ci.js"
  },
  "dependencies": {
    "@kispace-io/appspace": "1.1.29"
  }
}
